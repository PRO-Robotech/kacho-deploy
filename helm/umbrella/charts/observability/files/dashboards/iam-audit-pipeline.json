{
  "title": "Kachō IAM — Audit Pipeline (link)",
  "uid": "iam-audit-pipeline",
  "tags": ["kacho", "iam", "audit"],
  "schemaVersion": 39,
  "version": 1,
  "description": "Audit pipeline dashboard from Phase 9 — embedded via Grafana provisioning. See kacho-iam/files/grafana-dashboards/audit-pipeline.json for the canonical dashboard.",
  "panels": [
    {"id": 1, "type": "text", "title": "Audit Pipeline Reference", "gridPos": {"x": 0, "y": 0, "w": 24, "h": 4}, "options": {"content": "This Phase-11 dashboard links to Phase 9 audit pipeline panels. Canonical source: `charts/kacho-iam/files/grafana-dashboards/audit-pipeline.json`. Filter by `kacho_cloud_region` to inspect per-region Kafka ingest lag + ClickHouse fetch latency + S3 batch sign success rate.", "mode": "markdown"}},
    {"id": 2, "type": "graph", "title": "Kafka audit-events ingest lag (per region)", "datasource": "Mimir", "gridPos": {"x": 0, "y": 4, "w": 12, "h": 8}, "targets": [{"expr": "max by (kacho_cloud_region) (kafka_consumer_lag_ms{topic=\"audit-events\"})"}]},
    {"id": 3, "type": "stat", "title": "HSM batch sign success rate (24h)", "datasource": "Mimir", "gridPos": {"x": 12, "y": 4, "w": 6, "h": 4}, "targets": [{"expr": "sum(increase(kacho_audit_batch_sign_success_total[24h])) / sum(increase(kacho_audit_batch_sign_attempts_total[24h]))"}], "fieldConfig": {"defaults": {"unit": "percentunit"}}}
  ],
  "time": {"from": "now-6h", "to": "now"},
  "refresh": "1m"
}
