apiVersion: v2
name: litmus-chaos
version: 1.0.0
description: "Litmus 3.x chaos engineering subchart for Kachō — KAC-127 Phase 12. Quarterly game-day workflows + 12 ChaosExperiment CRDs covering pod-kill, network-partition, slow-postgres, kafka-broker-down, openfga-replica-down, dpop-cert-revocation, hsm-unavailable, cert-expiry, argocd-out-of-sync, caep-subscriber-down, dns-failure, node-drain. HA chaos-center 3 replicas. Database: separate Postgres kacho_litmus (запрет #8). Auth: OIDC via Hydra. Conditionally enabled via litmus-chaos.enabled in umbrella values (opt-in: chaos prep, not prod-default)."
keywords:
  - chaos-engineering
  - litmus
  - resilience
  - kacho
home: https://github.com/PRO-Robotech/kacho-deploy
sources:
  - https://github.com/litmuschaos/litmus
maintainers:
  - name: Kachō Platform Team
    email: platform@kacho.cloud
appVersion: "3.10.0"
