apiVersion: v2
name: kacho-iam
version: 1.2.0
appVersion: 1.2.0
type: application
description: "Kachō IAM — Account / Project / User / ServiceAccount / Group / Role / AccessBinding. KAC-127 Phase 3: OPA sidecar injection (deny-only Rego guardrails) + OpenFGA Authorization Model v2 (Conditions context) + bundle server for signed Rego policies."
